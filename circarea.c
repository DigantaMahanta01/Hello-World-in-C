#include<stdio.h>
int main(int argc, char const *argv[])
{
    float a=3.14;
    int r;
    printf("Enter the arc of the circle: ",r);
    scanf("%d",&r);
    printf("The area is: %f",(a * (r*r)));
    return 0;
}